负载均衡
作用：

分发策略：



请求：

​		动态请求：需要代码做业务逻辑处理。或需要读取数据库的请求，每个请求得到的结果不同
​		静态请求：只要是请求的 js,css,文件等 都是静态请求

​		静态服务器：用来解析或处理静态请求的服务器，如nginx,apache

​		动态服务器：用来解析或处理动态请求的服务器，如tomcat,weblogic 



代理：

​		正向代理：可见的，部署在请求端与服务端，起到请求和转发的作用

​		反向代理：



​		

