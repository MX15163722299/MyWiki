在 Python 项目中，**`requirements.txt`** 用于记录项目的依赖项，方便环境的复现和共享。以下是创建和管理 `requirements.txt` 的方法：

------

## **📌 1. 生成 `requirements.txt`**

### **方法 1：自动生成（推荐）**

如果你已经安装了依赖，可以用 `pip freeze` 生成：

```sh
pip freeze > requirements.txt
```

- 这个命令会**列出所有已安装的包**及其版本，并写入 `requirements.txt`。

### **方法 2：手动编写**

如果你想手动管理，可以直接编辑 `requirements.txt`，格式如下：

```
Flask==2.0.1
requests>=2.26.0
numpy
```

- `==` 指定确切版本
- `>=` 指定最低版本

------

## **📌 2. 安装 `requirements.txt` 中的依赖**

在新的环境中安装所有依赖：

```sh
pip install -r requirements.txt
```

这会自动安装 `requirements.txt` 中列出的所有依赖。

------

## **📌 3. 更新 `requirements.txt`**

如果你安装了新依赖，想更新 `requirements.txt`：

```sh
pip freeze > requirements.txt
```

这样 `requirements.txt` 会同步最新的依赖项。

------

## **📌 4. `requirements.txt` 配合虚拟环境**

**避免上传 `venv/`，而是用 `requirements.txt` 记录依赖**

1. 创建虚拟环境

   ```sh
   python -m venv venv
   ```

2. 激活虚拟环境

   - Windows

     ：

     ```sh
     venv\Scripts\activate
     ```

   - Mac/Linux

     ：

     ```sh
     source venv/bin/activate
     ```

3. 安装依赖并写入 `requirements.txt`

   ```sh
   pip install flask requests numpy
   pip freeze > requirements.txt
   ```

4. 在新的环境中，直接使用 `requirements.txt` 安装

   ```sh
   pip install -r requirements.txt
   ```

------

## **🚀 最佳实践**

✅ **不上传 `venv/`，只上传 `requirements.txt`**
 ✅ **定期更新 `requirements.txt` 以保证依赖一致**
 ✅ **使用虚拟环境，确保隔离不同项目的依赖**

这样，你的团队或服务器可以**快速复现环境**，避免依赖冲突！ 🚀