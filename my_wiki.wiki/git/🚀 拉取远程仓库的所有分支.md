要拉取远程仓库的所有分支，可以使用以下命令：

### **🚀 拉取远程仓库的所有分支**

```bash
git fetch --all
```

这个命令会从远程仓库拉取所有分支的最新状态，但不会自动切换到这些分支。

------

### **🚀 查看远程所有分支**

```bash
git branch -r
```

这会列出所有远程分支，例如：

```
origin/main
origin/dev
origin/feature-branch
```

------

### **🚀 切换到某个远程分支**

如果你想切换到某个远程分支（比如 `dev` 分支），可以运行：

```bash
git checkout -b dev origin/dev
```

或者在 Git 2.23+ 版本中，推荐使用：

```bash
git switch -c dev origin/dev
```

------

### **🚀 拉取所有分支的最新代码**

```bash
git pull --all
```

但请注意，这不会自动合并所有分支，而是更新你的远程追踪分支。

------

### **🚀 获取远程所有分支并在本地创建对应分支**

如果你想在本地创建所有远程分支的本地分支，可以运行：

```bash
git branch -r | grep -v '\->' | while read remote; do git branch --track "${remote#origin/}" "$remote"; done
```

然后再执行：

```bash
git fetch --all
git pull --all
```

------

### **🎯 总结**

| **操作**             | **Git 命令**                                  |
| -------------------- | --------------------------------------------- |
| 拉取远程所有分支     | `git fetch --all`                             |
| 查看远程分支         | `git branch -r`                               |
| 切换到远程分支       | `git checkout -b <branch> origin/<branch>`    |
| 更新所有分支         | `git pull --all`                              |
| 本地创建所有远程分支 | `git branch --track <branch> origin/<branch>` |

这样，你就可以拉取和管理远程仓库的所有分支了！🚀

